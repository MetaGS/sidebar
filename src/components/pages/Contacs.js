import React, {Component} from 'react';
import styles from './styles.module.css';

const Contacts = (props) => {
    return (
        <div className={styles.main}>
            here is Contacst page will be need to rearrange this
        </div>

    )
}

export default Contacts;