import React from 'react';
import CloseButton from '../components/sidebar/closeButton';

export default {
    title:'closeButton',
    component: CloseButton
}

export const Button = () => <CloseButton/>